define([], function() {
  return {
    "PropertyPaneDescription": "Settings for QR code",
    "UrlFieldLabel": "URL for QR code (will use current url if empty)",
    "TextFieldLabel": "Link text (describe what happens when you scan the code)",
  }
});
